/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: false,
	// output: 'export',
	// swcMinify: true,

	// // Add basePath
	// basePath: '/github-pages',
}

module.exports = nextConfig
